---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<hr class={`my-8 h-px w-full border-0 bg-gradient-to-r from-teal-200 via-teal-400 to-teal-200 rounded ${className ?? ""}`} />
